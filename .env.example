# Porta em que o servidor Express sobe
PORT=3000

# "production" ativa o cookie de sessão com flag Secure (exige HTTPS)
# use "development" enquanto testa localmente
NODE_ENV=development

# Senha única do painel admin. Troque antes de subir em produção.
ADMIN_PASSWORD=troque-esta-senha-agora
